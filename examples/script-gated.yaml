version: 1
id: script-gated-demo
name: Script Gated Demo
status: active

triggers:
  - type: script
    every: 1m
    command: ["@config/scripts/script-gated-demo/always-run.sh"]
    timeout: 10s

run:
  command: ["@config/scripts/script-gated-demo/run.sh"]
  timeout: 1m

env:
  TICKLE_DEMO: "1"
